#include "pins_arduino.h"
#include "easySPI.h"

/*
easySPI::easySPI(byte modeclk)
{
  pinMode(pinSCK, OUTPUT);
  pinMode(pinMOSI, OUTPUT);
  pinMode(pinSS, OUTPUT);
  pinMode(pinMISO, INPUT);

  _mode = modeclk | _BV(MSTR) | _BV(SPE);
  spi_begin();
}
*/
